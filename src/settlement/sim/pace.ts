/**
 * Real seconds each second of building work takes: raising, taking down,
 * clearing ruins and repairing (`KINDS` labor), paving and fencing. It sets
 * how long a building stays under its scaffold, and so how fast the
 * settlement grows in real time, without changing how fast anyone walks or
 * swings a tool.
 */
export const TOIL = 17;
